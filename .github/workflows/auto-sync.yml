name: Auto Sync Correoto

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Auto Sync
        run: |
          echo "🔄 Sincronização automática ativada"
          echo "📁 Estrutura do projeto:"
          ls -la